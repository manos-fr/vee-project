table:
  name: users
  schema: public
array_relationships:
  - name: users_grants
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: users_grants
          schema: public
